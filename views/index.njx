{% extends "layout.njx" %}

{% block content %}
  
  <div class="hero">
    <div class="title">
      <i class="fa fa-instagram" aria-hidden="true"></i>
      <h3>{{ title }}</h3>
    </div>
    <button> <a href="/authorize_user">New Game</a></button>
  </div>

  <div id="container">
    <div id="board">
      {% for image in userImages %}
        <div><img src="{{ image.images.thumbnail.url}}"></div>
      {% endfor %}
    </div>
  </div>
  

 
{% endblock %}